# Copy this file to .env.local and fill in your values
# Never commit .env.local to git

VITE_DRIVE_FILE_ID=replace_with_your_google_drive_file_id
VITE_DRIVE_API_KEY=replace_with_your_google_drive_api_key
VITE_APP_NAME=Smart Quote Generator

# Auth credentials — set these in Vercel dashboard / GitHub secrets
# These are server-side only and never exposed to the client
APP_USERNAME=mbmbinu
APP_PASSWORD=replace_with_your_password
